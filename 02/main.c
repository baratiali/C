#include <stdio.h>


int main()

{

    int x; //declraing the variable 
    x = 10; // variable initialization
    int y = x / 2;

	//printf(y);
    
    printf("the magic nummber is: %i\nThe value of x is %i\n",y, x);//"%i" is format strign, because we need to convert the number to form that consule allows

	
    
    
    
    return 0;

}